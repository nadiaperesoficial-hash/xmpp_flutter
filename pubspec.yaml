name: simple_chat
description: Simple chat application utilizing Xmpp Stone library

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^3.0.0  # Para conectar na URL wss:// do Hugging Face
  xml: ^6.5.0                 # Para ler e estruturar as tags XML do XMPP
  whixp: ^2.1.3
  rxdart: ^0.27.7
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  get_it: ^7.6.4
  sqflite: ^2.3.0
  shared_preferences: ^2.2.2
  path: ^1.8.0
  quiver: ^3.2.1
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
